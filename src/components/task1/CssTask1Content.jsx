export default function CssTask1Content() {
  return (
    <>
      <p>
        CSS (Cascading Style Sheets) is used to control the visual appearance
        of web pages. It works alongside HTML to style layouts, colors, fonts,
        spacing, and responsiveness.
      </p>

      <p>
        CSS follows a rule-based syntax where selectors target HTML elements
        and apply styles through properties and values.
      </p>

      <p>
        Understanding CSS is essential for building visually appealing and
        accessible user interfaces.
      </p>

      <ul>
        <li>CSS syntax: selectors, properties, values</li>
        <li>Colors, fonts, spacing, and borders</li>
        <li>Box model and layout basics</li>
      </ul>

      <h3>Why CSS Matters</h3>
      <p>
        Without CSS, websites would be plain text. CSS enables consistency,
        branding, responsiveness, and better user experience.
      </p>

      <h3>Real-World Usage</h3>
      <p>
        CSS is used in websites, dashboards, mobile layouts, email templates,
        and modern UI frameworks.
      </p>
    </>
  );
}