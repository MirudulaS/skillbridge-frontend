export default function HtmlTask1Content() {
  return (
    <>
      <p>
        HTML (HyperText Markup Language) is the foundational language of the web.
        Every website you see in a browser is structured using HTML.
      </p>

      <p>
        HTML uses elements (tags) to describe content such as headings,
        paragraphs, images, links, lists, and forms.
      </p>

      <p>Learning HTML is mandatory before CSS or JavaScript.</p>

      <ul>
        <li>HTML document structure</li>
        <li>Headings, paragraphs, and formatting</li>
        <li>Links, images, and layout elements</li>
      </ul>

      <h3>Why HTML Matters</h3>
      <p>
        HTML structure impacts accessibility, SEO, and maintainability.
        Strong HTML fundamentals make every frontend developer better.
      </p>

      <h3>Real-World Usage</h3>
      <p>
        HTML is used in websites, dashboards, emails, documentation portals,
        and embedded systems with web interfaces.
      </p>

      <h3>How Browsers Read HTML</h3>
      <p>
        Browsers parse HTML from top to bottom and build the DOM
        (Document Object Model).
      </p>
    </>
  );
}
